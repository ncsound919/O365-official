/**
 * Strategist source — social replies. STUB ONLY.
 * Depends on Connector/Pulse marketing agents being live first.
 */
import type { EvidenceItem } from "../../shared/types.js";

export function fetchSocialReplies(): Promise<EvidenceItem[]> {
  throw new Error(
    "socialReplies: depends on Connector/Pulse agents being live — out of scope for this build."
  );
}
